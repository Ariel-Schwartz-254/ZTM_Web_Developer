CSS Cheat Sheet

Reference:
*https://www.w3schools.com/cssref/css_selectors.asp
*https://css-tricks.com/almanac/

Cascading Style Sheets at the most basic level it indicates that the order of CSS rules matter. 


.class
#id
*
element
element, element (Both)
element element (Within)
element > element (Direct descendant)
element + element (match the combination)
:hover (When hovering wih the cursor)
:last-child (last child elements)
:first-child (first child elements)
!important (not recommended)


What seletors win out in the cascade depends on:
-Specificity
-Importance
-Source Order